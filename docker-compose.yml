version: '3.8'

services:
  morley-ai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: morley-ai-web
    ports:
      - "3000:3000"
    restart: always
    environment:
      - NODE_ENV=production
